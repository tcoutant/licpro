#include <stdio.h>

int main()
{
  int a,b,c,n ;

  printf("entrer un entier: ") ;
  scanf("%d",&n) ;

  for (c=1 ; c<=n ; c++)
    for (b=1 ; b<c ; b++)
      for (a=1 ; a<=b ; a++)
        if (a*a+b*b==c*c) printf("%d,%d,%d\n",a,b,c) ;

  return 0 ;
}

