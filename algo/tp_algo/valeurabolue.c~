# include <stdio.h>
# include <math.h>
int nb,a,b;
int valeur_absolue(int a)
	{
	if(a<0) return -a;
	else return a;
	}
void main(int argc, char * argv[])  
{
printf(" valeur absolue de %d est : %d\n", -4,valeur_absolue(-4));
}
