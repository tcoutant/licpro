# include <stdio.h>
# define size 10
int i, tab[size];
void main()
{
for(i=0;i<size;i++)
{
printf("Donner le port N°%d :\n",i);
scanf("%d",&tab[i]);
}
// controle des ports reservés
for (i=0;i<size;i++)
	if (tab[i]<=1000)
	printf("le port %d est réservé. \n",tab[i]);
}

