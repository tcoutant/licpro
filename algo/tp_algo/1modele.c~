# include <stdio.h>
int nb;	//declaration des variables//
int Factorielle(int a)
{
	if (a==0)
	return 1;
	else
	return a*Factorielle(a-1);
}

void main()			//programme principal//
{
printf("la factorielle de 5 est égale à %d :\n",Factorielle(5));
}
